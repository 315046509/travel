<script>
    <% if flash[:note]%>
    alert('<%=flash[:note]%>');
    <% end%>
</script>
<div>
  <ul class="breadcrumb">
    <li>
      <a href="#">Home</a> <span class="divider">/</span>
    </li>
    <li>
      世界游列表
    </li>
  </ul>
</div>
<div class="row-fluid sortable">
  <div class="box span12">
    <div class="box-header well" data-original-title>
      <h2><i class="icon-list-alt"></i> 世界游列表</h2>
    </div>
    <div class="box-content">
      <div class="form-actions">
        <div style="float: left">
          <%= link_to '添加城市', new_admin_city_path, :class => "btn btn-primary" %>
        </div>
      </div>
      <table class="table table-striped table-bordered bootstrap-datatable datatable">
        <thead>
        <tr>
          <th>ID</th>
          <th>分类</th>
          <th>城市</th>
          <th>特价旅游</th>
          <th>热门旅游推荐</th>
          <th>跟团游热门地区</th>
          <th>自由行</th>
          <th>修改</th>
        </tr>
        </thead>
        <tbody>
        <% @jobs.each do |q| %>
            <tr>
              <td><%= q.id %></td>
              <td><%= q.question_category_name %></td>
              <td><%= q.title %></td>
              <td><%= link_to '特价旅游', admin_city_tejia_path(q), :class => "btn btn-primary" %></td>
              <td><%= link_to '热门旅游推荐', admin_city_recommends_path(q), :class => "btn btn-primary" %></td>
              <td><%= link_to '跟团游热门地区', admin_city_gentuans_path(q), :class => "btn btn-primary" %></td>
              <td><%= link_to '自由行', admin_city_freelines_path(q), :class => "btn btn-primary" %></td>
              <td><%= link_to '修改名称', edit_admin_city_path(q), :class => "btn btn-primary" %></td>
            </tr>
        <% end %>
        </tbody>
      </table>
      <div class="pagination pagination-centered">
        <ul>
          <%= paginate @jobs %>
        </ul>
      </div>
    </div>
  </div>
  <!--/span-->
</div><!--/row-->